class Sound {
		this.tracks = [];
		for (let i = 0; i < 5; i++) {
			this.tracks[i] = loadSound('../../Assets/'+i'+_tracks.mp3');
		}
		this.
		this.isOn = false;

		play(songNumber){

			trackNumber === undefined ? trackNumber = 0 : songNumber;
			while (this.isOn == true) {
				this.tracks[trackNumber].play 
			}
		}
}

